import { Box, Text } from "@chakra-ui/react";
import React from "react";
import { useMemo } from "react";

export const Logo = () => {
  return (
    <Box height={40} width={40} backgroundColor={"#2979FF"}>
      <Text>S</Text>
    </Box>
  );
};
